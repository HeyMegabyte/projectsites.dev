import { Component } from '@angular/core';

/**
 * Deprecated: Toast notifications are now handled via Ionic ToastController
 * in ToastService. This component is kept as a no-op for import compatibility.
 */
@Component({
  selector: 'app-toast',
  standalone: true,
  template: '',
})
export class ToastComponent {}
